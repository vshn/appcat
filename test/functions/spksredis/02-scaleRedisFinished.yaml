desired:
  resources:
    redis-chart:
      resource:
        apiVersion: helm.crossplane.io/v1beta1
        kind: Release
        spec:
          forProvider:
            values:
              haproxy:
                tls:
                  enabled: true
              replicas: 0

input:
  apiVersion: v1
  kind: ConfigMap
  metadata:
    annotations: {}
    labels:
      name: xfn-config
    name: xfn-config
  data:
    controlNamespace: appcat-control

observed:
  resources:
    redis-chart:
      resource:
        apiVersion: helm.crossplane.io/v1beta1
        kind: Release
        spec:
          forProvider:
            values:
              haproxy:
                tls:
                  enabled: true
              replicas: 0
