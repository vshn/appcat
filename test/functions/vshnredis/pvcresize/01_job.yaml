desired:
  composite:
    resource:
      apiVersion: vshn.appcat.vshn.io/v1
      kind: XVSHNRedis
      metadata:
        creationTimestamp: "2023-03-21T16:52:31Z"
        finalizers:
        - composite.apiextensions.crossplane.io
        generateName: redis-
        generation: 13
        labels:
          appuio.io/organization: vshn
          crossplane.io/claim-name: redis
          crossplane.io/claim-namespace: unit-test
          crossplane.io/composite: redis-gc9x4
        name: redis-gc9x4
      spec:
        parameters:
          service:
            version: "7.0"
          size:
            disk: 16Gi
        writeConnectionSecretToRef: {}
      status: {}
  resources:
    release:
      resource:
        apiVersion: helm.crossplane.io/v1beta1
        kind: Release
        spec:
          forProvider:
            chart:
              name: redis
              repository: https://charts.bitnami.com/bitnami
            values:
              architecture: standalone
              commonConfiguration: dummy
              fullnameOverride: redis
              global:
                imageRegistry: dummy
              image:
                repository: bitnami/redis
                tag: 7.0.11
              replica:
                containerSecurityContext:
                  enabled: true
                persistence:
                  size: 16Gi
                podSecurityContext:
                  enabled: true
                resources:
                  limits:
                    cpu: 10m
                    memory: 1Gi
                  requests:
                    cpu: 10m
                    memory: 1Gi
              networkPolicy:
                allowExternal: false
                enabled: true
                ingressNSMatchLabels:
                  kubernetes.io/metadata.name: dummy
              tls:
                authClients: true
                autoGenerated: false
                certCAFilename: ca.crt
                certFilename: tls.crt
                certKeyFilename: tls.key
                enabled: true
                existingSecret: tls-server-certificate
          providerConfigRef:
            name: helm
observed:
  composite:
    resource:
      apiVersion: vshn.appcat.vshn.io/v1
      kind: XVSHNRedis
      metadata:
        creationTimestamp: "2023-03-21T16:52:31Z"
        finalizers:
        - composite.apiextensions.crossplane.io
        generateName: redis-
        generation: 13
        labels:
          appuio.io/organization: vshn
          crossplane.io/claim-name: redis
          crossplane.io/claim-namespace: unit-test
          crossplane.io/composite: redis-gc9x4
        name: redis-gc9x4
      spec:
        parameters:
          service:
            version: "7.0"
          size:
            disk: 16Gi
        writeConnectionSecretToRef: {}
      status: {}
  resources:
    redis-gc9x4-sts-deleter:
      resource:
        apiVersion: kubernetes.crossplane.io/v1alpha1
        kind: Object
        metadata:
          creationTimestamp: null
          name: redis-gc9x4-sts-deleter
        spec:
          forProvider: null
          providerConfigRef:
            name: kubernetes
        status:
          atProvider:
            manifest:
              apiVersion: batch/v1
              kind: Job
              metadata:
                creationTimestamp: null
                name: redis-gc9x4-sts-deletion-job
              spec:
                template:
                  metadata:
                    creationTimestamp: null
                  spec:
                    containers: null
              status:
                succeeded: 1
    redis-gc9x4-sts-observer:
      resource:
        apiVersion: kubernetes.crossplane.io/v1alpha1
        kind: Object
        metadata:
          creationTimestamp: null
          name: redis-gc9x4-sts-observer
        spec:
          forProvider: null
          providerConfigRef:
            name: kubernetes
        status:
          atProvider:
            manifest:
              apiVersion: apps/v1
              kind: StatefulSet
              metadata:
                creationTimestamp: null
                name: redis-gc9x4-sts-observer
              spec:
                volumeClaimTemplates:
                - spec:
                    resources:
                      requests:
                        storage: 16Gi
              status:
                readyReplicas: 1
    release:
      resource:
        apiVersion: helm.crossplane.io/v1beta1
        kind: Release
        spec:
          forProvider:
            chart:
              name: redis
              repository: https://charts.bitnami.com/bitnami
            values:
              architecture: standalone
              commonConfiguration: dummy
              fullnameOverride: redis
              global:
                imageRegistry: dummy
              image:
                repository: bitnami/redis
                tag: "7.0"
              replica:
                containerSecurityContext:
                  enabled: true
                extraVolumes: []
                persistence:
                  size: 16Gi
                podSecurityContext:
                  enabled: true
                resources:
                  limits:
                    cpu: 10m
                    memory: 1Gi
                  requests:
                    cpu: 10m
                    memory: 1Gi
              networkPolicy:
                allowExternal: false
                enabled: true
                ingressNSMatchLabels:
                  kubernetes.io/metadata.name: dummy
              tls:
                authClients: true
                autoGenerated: false
                certCAFilename: ca.crt
                certFilename: tls.crt
                certKeyFilename: tls.key
                enabled: true
                existingSecret: tls-server-certificate
          providerConfigRef:
            name: helm

